package domain;

/** There are in fact only about 250 countries
 * so it should be possible to maintain an enum.
 */
public enum Country {
	CANADA,
	AUSTRALIA,
	MEXICO,
	FRANCE,
	GERMANY,
	UK,
	USA
}
